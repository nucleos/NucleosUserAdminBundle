# These settings are synced to GitHub by https://probot.github.io/apps/settings/

_extends: .github

repository:
  name: NucleosUserAdminBundle
  description: 👤 Adminstration of users and groups in symfony
  homepage: https://docs.nucleos.rocks/projects/user-admin-bundle/
  topics: symfony, symfony-bundle, bundle, user, management, admin, sonata

branches:
  - name: main
    protection:
      enforce_admins: false
      required_status_checks:
        contexts:
          - "Coding Standards (7.4)"
          - "Static Code Analysis (7.4)"
          - "Tests (7.3, lowest, ~4.4)"
          - "Tests (7.3, highest, ~4.4)"
          - "Tests (7.4, lowest, ~4.4)"
          - "Tests (7.4, highest, ~4.4)"
          - "Code Coverage (7.4)"
          - "Mutation Tests (7.4)"
        strict: true

      required_pull_request_reviews: null
      restrictions: null
